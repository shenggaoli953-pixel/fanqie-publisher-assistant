# 首个发布日期设计

每本作品保存一个可选的首个自动发布日期。设置日期后，未提交章节从该日期的设定时刻开始逐日排程；留空表示暂不发布，列表不显示可发布批次，工具也不能确认提交。

添加作品窗口默认保持“暂不发布”，用户可用日期选择器选定未来日期。已添加作品可在主界面修改日期或清空日期，保存后仅重排未提交章节；已提交章节的原定时记录不变。

不接入番茄后台，不提交章节。本次仅改本地排程和状态控制。
